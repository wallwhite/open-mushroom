# Transitive dev tooling (ast-kit 3 and the Babel 8 pre-releases behind tsdown's declaration plugin) declares
# Node >=22.18 while local machines may still run an older Node 22; the root engines field stays enforced and CI
# runs the latest Node 22 from .nvmrc. Keep workspace ranges on versions verified locally.
engine-strict=false
