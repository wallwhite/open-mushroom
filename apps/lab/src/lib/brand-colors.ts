/*
 * The palette tokens as plain sRGB, for the surfaces that cannot read CSS: generated
 * images and the browser's own chrome. Keep in step with the oklch tokens in globals.css.
 */
export const BRAND_COLORS = {
  background: '#f3f3f6',
  foreground: '#18181d',
  muted: '#686870',
  brand: '#fe860f',
} as const;
