'use client';

import { Mushroom } from 'open-mushroom';

/* The whole integration: one component, one emotion, one size. */
export const BasicExample = () => <Mushroom emotion="neutral" size={160} />;
